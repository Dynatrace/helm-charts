Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To find more information about the Dynatrace ActiveGate Operator, try:
https://github.com/Dynatrace/dynatrace-activegate-operator

To verify the current state of the OneAgent deployment, try:
  $ kubectl get pods -n {{ .Release.Namespace }}
  $ kubectl logs -f deployment/{{ .Release.Name }} -n {{ .Release.Namespace }}
  $ kubectl get activegate {{ .Values.activegate.name }} -n {{ .Release.Namespace }}