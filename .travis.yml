---
language: python
install:
  # install helm
  - curl -fsSL -o get_helm.sh https://raw.githubusercontent.com/helm/helm/master/scripts/get-helm-3
  - chmod 700 get_helm.sh
  - ./get_helm.sh
  # install yamllint
  - sudo pip install yamllint

jobs:
  include:
    - stage: test
      script: exit 0
    - stage: lint
      script: cd dynatrace-oneagent-operator && helm template --debug . | yamllint -
